**Unreleased**

* Validate and URL-encode Elasticsearch index names before constructing search paths.
* Escape widget values before embedding them in inline JavaScript.
* Enable Elasticsearch TLS certificate verification by default.
